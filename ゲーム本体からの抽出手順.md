# ゲーム本体からの抽出手順（日本語テキスト + アセット画像）

自宅のゲーミングPCで一度だけ行う作業。日本語ローカライズと画像を同時に取得する。

---

## 事前に分かっていること

**画像がVPKの中に実在することは確認済み。** GameTracking-Deadlock に
`game/citadel/pak01_dir.txt`（VPKの中身の全ファイル一覧・130,731件）が含まれており、
サイトが必要とする **393件すべての存在を照合済み**（不一致0件）。

パスの対応規則も実物で確認した:

| | 例 |
|---|---|
| データ側の参照 | `file://{images}/items/weapon/basic_magazine.psd` |
| VPK内 | `panorama/images/items/weapon/basic_magazine_psd.vtex_c` |
| 出力先 | `public/images/items/weapon/basic_magazine.png` |

ラスタ画像は拡張子が `_psd` としてファイル名に残る。SVGだけは残らない
（`icon_speed.svg` → `icon_speed.vsvg_c`）。

必要な393件の合計は圧縮前で約58MB。

**まだ確認できていないこと**: 変換ツールがこれらの `.vtex_c` を実際にPNGとして
書き出せるかどうか。これは手元でしか試せないので、**手順1で最初に確かめる**。
ここで失敗したら画像は諦めて、こちらでアセットを用意する方針に切り替える。

---

## 手順0: 日本語テキストのコピー（画像より先に、こちらだけでも価値がある）

1. Steam → ライブラリ → Deadlock を右クリック → 「管理」→「ローカルファイルを閲覧」
2. `game` → `citadel` → `resource` → **`localization` フォルダをコピー**
3. OneDrive の `ドキュメント\Deadlock-jp.wiki\` に **`game-localization`** の名前で貼り付け

`citadel_gc_mod_names_japanese.txt` のような `_japanese` 付きファイルがあれば成功。
無ければ Steam の Deadlock のプロパティ → 言語 を日本語にしてから再試行。

---

## 手順1: 変換できるか最初に確かめる（5分）

**全部やる前に1枚だけ試す。** ここが通らなければ以降は無意味なので、必ず先に行う。

1. **Source 2 Viewer** を入手する（ValveResourceFormat プロジェクトの公開ビルド）
2. 起動して、手順0で開いたフォルダの **`game/citadel/pak01_dir.vpk`** を開く
3. 左のツリーで `panorama` → `images` → `items` → `weapon` をたどる
4. **`basic_magazine_psd.vtex_c`** を選ぶ
5. プレビューに拡張マガジンのアイコンが表示されるか確認する
6. 右クリック → Export / Decompile で **PNG として書き出す**
7. 書き出したPNGを画像ビューアで開き、**アイコンとして見えるか**確認する

### 判定

- **アイコンが見えた** → 手順2へ進む
- **プレビューが真っ黒・エラー・PNGが壊れている** → **ここで中止**。
  その旨と、出たエラーメッセージを教えてください。画像は自前で用意する方針に切り替えます。

---

## 手順2: 必要な393件を書き出す

`data/image-manifest.json` に、必要なVPK内パスと出力先の対応が全393件入っている。

Source 2 Viewer で以下のフォルダをまとめて Export する（余分に取れても構わない。
後で照合スクリプトが必要なものだけを判定する）:

- `panorama/images/items/` （181件・アイテムアイコン）
- `panorama/images/upgrades/` （アイテムアイコンの別系統）
- `panorama/images/hud/abilities/` （232件・スキルアイコン）
- `panorama/images/hud/icons/`
- `panorama/images/heroes/` （490件・ヒーローカード等。使うのは一部）

書き出し先はどこでもよい。フォルダごと OneDrive の
`ドキュメント\Deadlock-jp.wiki\` に **`game-images`** の名前で置く。

---

## 手順3: 照合（こちらで実行）

`game-images` と `game-localization` が届いたら、こちらで:

1. ファイル名を `basic_magazine_psd.png` → `basic_magazine.png` に整えて
   `public/images/` 以下に配置
2. `npm run parse` 相当の照合を実行し、**393件中何件揃ったか**を機械的に確認
3. 足りないものがあればファイル名を挙げて報告

推測での紐付けは一切しない。全部パスで一致させる。

---

## うまくいかなかった場合

画像は自前の記号で代替する。TYPE色・ティア・頭文字を使ったバッジをCSSで作る。
`shopIcon` のパスはデータに残っているので、**後から実画像に差し替えるときは
アイコン部分のコンポーネント1つを差し替えるだけ**で済むように作ってある。

---

## 権利について

抽出した画像はValveの著作物。数値データ（事実なので著作権が及ばない）とは性質が違う。
ファンサイトでゲームのアイコンを使うのは広く行われている慣行だが、
そこは把握したうえでの判断になる。
