# ステータス計算式 実測チェックリスト v2

v1（`実測チェックリスト_記入済み.md`）で購入ボーナスの正体が判明し、
`m_MapModCostBonuses` の全33値がゲーム内表示と一致することを確認できた。
v2では**v1で確定できなかった3点**だけを潰す。所要時間はおよそ10分。

## 準備（v1と同じ）

- **ヒーロー: Inferno（インファーナス）**。基礎HP 830、レベルごとに +39。
- **サンドボックス / ヒーローテスト**。
- 表示された数値を**そのまま**書き写す。丸めや補正はしない。

## v1で分かっていること（前提）

- Boon = 標準レベルアップ1回。Lv1→2で最大HP +39、スピリットパワー +1.1。
- 購入ボーナスは**カテゴリへの累計投資額**で決まる階段関数。アイテムの個数・ティアは無関係。
- バイタリティ投資額 → 最大HP%: `800→+9% / 1,600→+12% / 2,400→+15% / 3,200→+20% / 4,800→+38% / 6,400→+42%`
- 平坦HPは%が掛かった**後**に足される。

---

## 測定A: Boon分は%の内側か外側か

v1は全てレベル1（Boon 0）で行ったため、レベルアップ分に%が掛かるのかが分かっていない。

**操作**

1. アイテム0個のまま、**レベルを4まで上げる**（最大HPが 947 になっているはず）
2. **Extra Stamina**（`upgrade_improved_stamina`／スタミナ+1・スタミナ回復+12%）を1個買う
   - HPを一切持たないArmor T1。バイタリティ投資額は 800 → +9%

| 状態 | 最大HP |
|---|---|
| Lv4・アイテム0個 | |
| Extra Stamina 購入後 | |

**判定**

| 実測値 | 意味 |
|---|---|
| **1032** 前後 | %はBoon分にも掛かる（内側）<br>(830 + 117) × 1.09 = 1032.2 |
| **1022** 前後 | %は基礎HPにだけ掛かる（外側）<br>830 × 1.09 + 117 = 1021.7 |
| どちらでもない | 別の規則。値をそのまま記録してください |

---

## 測定B: %が2つ重なったときの合成規則

v1では効いていた%が投資額由来の1つだけだったため、加算合成か乗算合成かを判別できていない。
**自前で%を持つアイテム**を使って重ねる。

**操作**

1. アイテムを全部売る／リセットして **レベル1・アイテム0個**（最大HP 830）に戻す
2. **Colossus**（`upgrade_colossus`／Armor ティア4・6,400ソウル）を1個だけ買う
   - このアイテム自身が **最大HP +25%** を持つ
   - 同時にバイタリティ投資額が 6,400 になり、テーブル側の **+42%** も乗る

| 状態 | 最大HP |
|---|---|
| Lv1・アイテム0個 | |
| Colossus 購入後 | |

**判定**

| 実測値 | 意味 |
|---|---|
| **1386** 前後 | %は加算合成<br>830 × (1 + 0.42 + 0.25) = 1386.1 |
| **1473** 前後 | %は乗算合成<br>830 × 1.42 × 1.25 = 1473.3 |
| どちらでもない | 値をそのまま記録してください |

差が87あるので、丸めが入っても判別できます。

> Colossusはアクティブ効果も持っていますが、最大HP+25%は常時発動のパッシブなので
> アクティブを使う必要はありません。

---

## 測定C: 閾値と閾値の間はどうなるか

テーブルの各段（800 / 1,600 / 2,400 …）はv1で一致を確認済み。
しかし**段と段の間の投資額**でどうなるかは未確認。
実装では「閾値以下で最大の段を引く階段関数」と仮定しているので、それを検証する。

**操作**

1. **レベル1・アイテム0個**（最大HP 830）に戻す
2. **Spirit Resilience**（`upgrade_tech_purge`／Armor ティア3・3,200ソウル／戦闘外HP回復+3のみ）を買う
3. 続けて **Extra Stamina**（Armor ティア1・800ソウル）を買う
   - バイタリティ投資額の合計が **4,000** になる（3,200と4,800のちょうど中間ではない点に注意）

| 状態 | バイタリティ投資額 | 最大HP |
|---|---|---|
| アイテム0個 | 0 | |
| Spirit Resilience 購入後 | 3,200 | |
| Extra Stamina 購入後 | 4,000 | |

**判定**（3,200時点は +20% = 996 になるはず。まずこれで前提を確認）

| 4,000時点の実測値 | 意味 |
|---|---|
| **996** のまま | 階段関数。閾値以下で最大の段を引く（実装の仮定どおり） |
| **1145** 前後 | 次の段へ切り上げ（4,800の +38% を適用）<br>830 × 1.38 = 1145.4 |
| **1071** 前後 | 段の間を補間している<br>+20%と+38%の中間 +29% なら 830 × 1.29 = 1070.7 |
| どちらでもない | 値をそのまま記録してください |

**あわせて記録してほしいこと**

投資額 4,000 のとき、画面のバイタリティ投資額パネルで
**どの行がハイライトされているか**（`3.2K` か `4.8K` か、あるいはどこにも無いか）。

---

## 測定D: ★行の正体（分かる範囲で）

データには各段に `flPercentOnGraph` という別の値が入っている
（バイタリティなら 7, 7, 8, 8, 9, 9, 10, 10, 10, 11, 11）。
UIで 4.8K の行に★が付く理由がこれかどうかを確かめたい。

**記録してほしいこと**（測定不要・観察のみ）

- ★は3カテゴリとも 4.8K の行だけについているか。他の行にも何か印はあるか
- ★の行にマウスを合わせるとツールチップが出るか。出るなら文面をそのまま
- パネルに投資額と倍率以外の情報（グラフ、バーなど）が表示されているか

分からなければ「不明」で構いません。この項目は実装をブロックしません。

---

## 記入後

このファイルをそのまま返してください。判定と architecture.html への反映はこちらで行います。
