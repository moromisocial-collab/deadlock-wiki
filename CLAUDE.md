# プロジェクトルール

作業を始める前に、必ず `architecture.html` を読み、サイト全体の構成・データソース・執筆方針を把握すること。
このファイル自体には詳細を書かない。詳細はarchitecture.html側で管理し、変更があればarchitecture.htmlを更新する。

## このプロジェクトでよく頼まれること

- 「GameTracking-Deadlockの最新差分でdata/以下を更新して」
  → architecture.htmlのデータパイプラインに従い、該当ファイルを再パースしてdata/\*.jsonを更新し、
  変更前後のJSON差分からchangelogを自動生成する。

## 注意

- 解説・考察・攻略テキストは書かない。生データの日本語化と可視化に徹する
- ゲーム内の日本語表記はそのまま使う。自前で訳し直さない。
  著作権がValve Corporationにあることを明示した非公式ファンサイトとして公開している。
  権利表示はフッター・`/about/`・README.md・LICENSE の4か所にある。表示を消さないこと
- deadlock.wikiは「計算式・仕様の確認」と「パース結果の検算」には参照してよい。
  参照して得た仕様はarchitecture.htmlに自分の言葉で記録する。
  **あちらの文章そのものは転載しない**(Valveの著作物ではなく、そのサイトの著作物のため)
- IDはゲーム内部の実IDを使う(連番にしない。共有URLの互換性が壊れるため)
- pushするとGitHub Actionsが走り、そのまま公開サイトに反映される。
  data/以下や権利表示に触れる変更は、影響を確認してからpushすること
